# Config File -- Environment Variables Section

```yaml
env:
  FOO: bar
```

The global `env` section specifies environment variables that will be made available to:

* all test suites
* the installation stage
* the setup stage

